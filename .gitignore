# ── Dependencies ─────────────────────────────────────────────
node_modules/
.pnpm-store/

# ── Build output ─────────────────────────────────────────────
dist/
build/
coverage/
.vite/
*.tsbuildinfo

# ── Logs ─────────────────────────────────────────────────────
*.log
npm-debug.log*
logs/

# ── Local environment / secrets (never commit) ───────────────
.env
.env.*
!.env.example
*.local

# ── Database dumps / data ────────────────────────────────────
*.sql.bak
*.dump
*.sqlite
# Anchored to the repo root. Unanchored, this matched every `data/` at every depth — including
# `apps/api/src/curriculum/catalogue/data/`, which is source the API cannot boot without and
# which was therefore never committed.
/data/

# ── IDE / editor ─────────────────────────────────────────────
.idea/
.vscode/
*.iml
*.iws
*.ipr

# ── OS files ─────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Misc ─────────────────────────────────────────────────────
*.pid
.cache/

# ── Legacy tree (Java/Spring + Vite app, kept for reference) ─
legacy/backend/target/
legacy/frontend/dist/
legacy/frontend/node_modules/
legacy/backend/src/main/resources/static/
legacy/backend/src/main/resources/application-local.yml
legacy/backend/src/main/resources/application-prod.yml
