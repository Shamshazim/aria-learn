/** `SHOW` payload builders: one per visual kind, drawn from the item in play (P2H-10). */
export { buildVisual, firstVisualFor, visualsFor } from '@/curriculum/visuals/show-payloads';
export type { VisualRequest } from '@/curriculum/visuals/show-payloads';
