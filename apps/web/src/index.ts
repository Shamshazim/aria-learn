/**
 * Entry point for `@aria/web`.
 *
 * The React application, its router and the carried-forward session UI are built in P0-05
 * and P0-06; this file exists so the package typechecks as part of the workspace.
 */
export {};
