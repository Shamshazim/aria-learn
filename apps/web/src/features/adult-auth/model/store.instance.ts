import { createBrowserAdultTokenStore } from './adult-token-store';

/** The one adult token store the browser app uses. See the child equivalent for why. */
export const adultTokenStore = createBrowserAdultTokenStore();
